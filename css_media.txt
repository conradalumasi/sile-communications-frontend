@media (max-width: 900px) {
    .products-grid { grid-template-columns: repeat(2, 1fr); }
    .safaricom-grid { grid-template-columns: repeat(2, 1fr); }
    .safaricom-item { flex-direction: column; align-items: center; text-align: center; }
    .solar-systems-grid { grid-template-columns: 1fr; }
    .solar-system-card { flex-direction: column; }
    .solar-system-img { width: 100%; }
    .services-grid { grid-template-columns: 1fr; }
    .about-grid { grid-template-columns: 1fr; }
    .contact-grid { grid-template-columns: 1fr; }
    .requirements-grid { grid-template-columns: 1fr; }
    .cart-layout { grid-template-columns: 1fr; }
    .checkout-grid { grid-template-columns: 1fr; }
    .auth-modern-container { grid-template-columns: 1fr; }
    .auth-image-side { display: none; }
    .solar-hero-wrapper { grid-template-columns: 1fr; text-align: center; }
    .product-detail-layout { grid-template-columns: 1fr; padding: 20px; }

    /* Account page mobile layout - compact and professional */
    .account-mobile-tabs {
        display: flex;
    }

    .account-layout {
        grid-template-columns: 1fr;
        gap: 16px;
    }

    .account-sidebar {
        position: static;
        padding: 12px 16px;
        border-radius: 16px;
    }

    .account-sidebar .account-menu {
        display: none;
    }

    .account-user {
        display: flex;
        align-items: center;
        gap: 12px;
        text-align: left;
        margin-bottom: 16px;
        padding-bottom: 12px;
    }

    .user-avatar {
        margin: 0;
        width: 56px;
        height: 56px;
        flex-shrink: 0;
    }

    .user-avatar img {
        width: 100%;
        height: 100%;
        object-fit: cover;
    }

    .account-user h3 {
        font-size: 16px;
        margin-bottom: 2px;
    }

    .account-user p {
        font-size: 12px;
    }

    /* Compact horizontal menu – like tabs, no scrolling bar */
    .account-menu {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 8px;
        padding: 8px 0 4px;
        margin: 0;
        overflow-x: visible;
        border-top: 1px solid var(--border);
        border-bottom: 1px solid var(--border);
    }

    .account-menu li {
        flex: 0 0 auto;
        white-space: nowrap;
        width: auto;
        padding: 8px 14px;
        background: var(--bg-light);
        border-radius: 40px;
        margin-bottom: 0;
        font-size: 13px;
        font-weight: 500;
        transition: all 0.2s;
    }

    .account-menu li i {
        font-size: 14px;
        margin-right: 6px;
    }

    .account-menu li.menu-logout {
        background: #fee2e2;
        color: #dc2626;
    }

    .account-menu li.menu-logout i {
        color: #dc2626;
    }

    .account-section {
        padding: 20px 16px;
    }

    .dashboard-stats {
        grid-template-columns: 1fr 1fr;
        gap: 12px;
    }

    .stat-card {
        padding: 14px;
    }

    .stat-card i {
        font-size: 24px;
    }

    .stat-card p {
        font-size: 20px;
    }

    .order-categories {
        flex-wrap: wrap;
        justify-content: flex-start;
    }

    .order-cat-btn {
        padding: 6px 14px;
        font-size: 12px;
    }

    .order-card {
        padding: 14px;
    }

    .order-header {
        flex-direction: column;
        align-items: flex-start;
        gap: 8px;
    }

    .order-status {
        align-self: flex-start;
    }

    .form-row {
        grid-template-columns: 1fr;
        gap: 12px;
    }

    .form-group input,
    .form-group select {
        padding: 10px 12px;
    }

    .btn-primary {
        padding: 10px 20px;
        font-size: 14px;
    }

    /* Horizontal scrolling menu */
    .account-menu {
        display: flex;
        flex-direction: row;
        overflow-x: auto;
        gap: 8px;
        padding-bottom: 8px;
        scroll-snap-type: x mandatory;
        -webkit-overflow-scrolling: touch;
    }
    .account-menu li {
        flex: 0 0 auto;
        white-space: nowrap;
        width: auto;
        padding: 10px 16px;
        scroll-snap-align: start;
        background: var(--bg-light);
        border-radius: 40px;
        margin-bottom: 0;
    }
    .account-menu li.menu-logout {
        margin-top: 0;
        border-top: none;
        background: #fee2e2;
    }
    .account-section {
        padding: 20px;
    }

    /* Mobile navigation (hamburger) */
    .main-header,
    .header-bottom {
        overflow: visible;
    }
    .main-nav {
        display: flex;
        align-items: center;
        justify-content: flex-end;
        position: relative;
        width: 100%;
        min-height: 44px;
    }
    .mobile-menu-btn {
        display: flex;
        align-items: center;
        justify-content: center;
        background: none;
        border: none;
        color: white;
        font-size: 22px;
        cursor: pointer;
        padding: 10px;
        z-index: 1002;
        margin-left: auto;
        flex-shrink: 0;
    }
    /* Orphan duplicate nav blocks (legacy sync leftovers) */
    header ~ nav.main-nav {
        display: none !important;
    }
    .nav-links {
        display: none;
        position: absolute;
        top: 100%;
        left: 0;
        right: 0;
        background: var(--secondary);
        flex-direction: column;
        z-index: 1001;
        padding: 10px 0;
        box-shadow: 0 8px 20px rgba(0,0,0,0.2);
        max-height: 80vh;
        overflow-y: auto;
        -webkit-overflow-scrolling: touch;
    }
    .nav-links.active {
        display: flex;
    }
    .nav-links li {
        width: 100%;
    }
    .nav-links li a {
        display: block;
        padding: 12px 20px;
        width: 100%;
        color: rgba(255,255,255,0.85);
    }
    .dropdown-content {
        position: static;
        background: rgba(255,255,255,0.08);
        box-shadow: none;
        margin-left: 20px;
        border-top: none;
        display: none;
    }
    .dropdown.active .dropdown-content {
        display: block;
    }
    .dropdown-content a {
        color: rgba(255,255,255,0.75) !important;
        background: transparent !important;
        padding: 10px 16px !important;
    }
}
